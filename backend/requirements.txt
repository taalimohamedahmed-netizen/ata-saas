# ATA Backend — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18

# Database (async PostgreSQL)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Redis (sessions + queues)
redis[hiredis]==5.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# AI — Anthropic Claude
anthropic==0.42.0

# HTTP client (Shopify, WhatsApp, payments)
httpx==0.28.1

# Env / config
python-dotenv==1.0.1

# Logging
structlog==24.4.0
